<div style="float:right">
    The cart(shopping basket) :
    <?php
    foreach ($params['cart'] as $product):?>
        <?= $this->viewHelper::productImgTag($product['image_path'], $product['name']) ?><?= $product['count'] ?>
    <?php
    endforeach;
    ?>
</div>
